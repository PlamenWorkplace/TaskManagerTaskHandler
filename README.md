# Task Manager: Task Handler

The Task Handler for Task Manager processes incoming task commands for the correlated user.
It creates, reads, updates and deletes user tasks depending on the given command.
